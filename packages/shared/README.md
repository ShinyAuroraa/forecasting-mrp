# @forecasting-mrp/shared

Shared TypeScript types, constants, and utilities consumed by `@forecasting-mrp/web` and `@forecasting-mrp/api`.

## Usage

Referenced as `@forecasting-mrp/shared` via pnpm workspace protocol (`workspace:*`).

## Build

```bash
pnpm build
```
